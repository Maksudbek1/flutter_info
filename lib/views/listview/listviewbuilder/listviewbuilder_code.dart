import 'package:flutter/material.dart';

class ListViewBuilderCode extends StatelessWidget {
  const ListViewBuilderCode({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Image.asset(
        "assets/listviewbuilder.png",
        fit: BoxFit.cover,
        height: double.infinity,
      ),
    );
  }
}
